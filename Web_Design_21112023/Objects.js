/*
// Math.random 

// Generate a decimal number between 0 and 0.99
Math.random();

// Save it to a variable:
var decimal = Math.random();

// Log the value of decimal to the console
console.log(decimal);

// Log the value of decimal MULTIPLIED by 10:
console.log(decimal * 10);
*/

//============================================================
/*
// Random integer

// Get a random decimal number 0 and 0.99 and save it to a variable
var decimal = Math.random() * 10;

// Rounding up the value of decimal
var rounded = Math.ceil(decimal);

// Log the value of rounded to the console log
console.log(rounded)
*/


//============================================================
//String Methods
// indexOf returns the location of first position that matches a character
console.log("ho-ho-ho".indexOf("-"));
// lastIndexOf finds the last match, otherwise returns the same as indexOf.
console.log("ho-ho-ho".lastIndexOf("-"));
// Split method chops up the string into an array of sub-strings.
console.log("ho-ho-ho".split("-"));

//.toUpperCase and .toLowerCase
var greet = "Hello";
console.log(greet.toUpperCase());
console.log(greet.toLowerCase());

